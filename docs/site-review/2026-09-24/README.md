# myCHEF focused website review — 24 September 2026

Base: `4af9adfc6d0f8185b304ee974a7edcbc234c295b`. First batch: `/catering-dubai` copy only. This is a draft for owner approval, not a production release.

The live inventory contains 176 sitemap/indexable URLs, 55 noindex pages with HTML and the SPA-only thank-you route (56 support routes by intended policy). There are 92 customer-facing redirect aliases and 21 SEO utility aliases: 345 reviewed HTTP candidates in total. Every candidate returned HTTP 200 or a redirect. This is an automated HTTP/HTML baseline; it is not a claim that every page has received a human editorial or phone/desktop review.

The older register contains 173 indexable pages, 56 support pages and 91 redirects. Three new sitemap articles and one additional blog alias account for the customer-facing difference. The 21 SEO utility aliases are counted separately. The current route count is reconciled without changing indexing policy or redirects.

The proposed edit removes contradictory staffing promises, distinguishes delivered food / one-off chef dining / staffed events / recurring cooking, removes awkward search phrases and fixes the pricing-link sentence. The primary keyword, title, H1, metadata, images, quote calculations and enquiry/WhatsApp destinations are preserved. The four-way service comparison links to the existing dining page, which returned HTTP 200 and its expected canonical.

Read `exact-copy-changes.md` for every changed rendered paragraph, `coverage.csv` for the route ledger, `photo-shot-list.md` for the selective photo requests and `validation.json` for checks and limitations.

Already resolved in the current baseline: corporate examples are introduced as ideas/sample formats; birthday examples are described as illustrative briefs. The yacht retains 21 real-work images. Do not repeat the older proposed fixes as if these were still missing.

Pending facts: confirm staffed response hours (9am–9pm versus 9am–11pm Dubai time), access to consented real service photos, and the authoritative price/terms source. Reply-time claims remain unchanged until the owner confirms them. Yacht/birthday copy or layout work requires a separately approved batch and extra phone, enquiry and measurement QA. The homepage preservation instruction remains in force.

Further work: reconcile current private-chef H1 documentation with its approved design, investigate the pre-existing birthday wording-test failure, and check initial HTML on the SPA-only thank-you page. Do not automatically change customer copy to satisfy stale documentation. Remaining page families are recorded as pending, not fully reviewed.

Release gates: owner approval of exact copy; full browser prerender in a working build environment; desktop/phone preview; post-release checks of the live URL, metadata, images, internal links and enquiry path. No production form or message has been submitted. No ads, budgets or bids were changed.
